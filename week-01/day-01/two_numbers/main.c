#include <stdio.h>

int main() {
    int a = 22;
    int b = 13;
    printf("a + b: %d\n", a + b);
    printf("a * b: %d\n", a * b);
    printf("a / b: %d\n", a / b);
    printf("a / b: %f\n", (float)a / b);
    return 0;
}